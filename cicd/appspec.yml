version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/aws-devops-zero-to-hero

hooks:
  BeforeInstall:
    - location: scripts/stop_container.sh
      timeout: 300
      runas: root

  ApplicationStart:
    - location: start_container.sh
      timeout: 300
      runas: root

